# <%= project_name %>

Generated by Static Ruby <%= gem_version %>.

## Commands

- `bundle exec static_ruby build` — regenerate the site into `build/`
- `bundle exec static_ruby serve` — serve `build/` with automatic rebuilds
- `bundle exec static_ruby menu` — refresh `docs/menu.yml`
- `bundle exec static_ruby test` — run the Minitest suite (if present)

Docs live in `docs/`, assets in `assets/`, templates in `templates/`.
