#!/usr/bin/env ruby
# frozen_string_literal: true

lib_path = File.expand_path('../lib', __dir__)
$LOAD_PATH.unshift(lib_path) if Dir.exist?(lib_path)

require 'static_ruby'

StaticRuby::Paths.reset!
StaticRuby::Paths.root = Dir.pwd

StaticRuby::CLI.start(ARGV)
